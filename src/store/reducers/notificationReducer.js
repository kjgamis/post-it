const initState = {
    notifications: [
        {id: 1, user: 'KJG', content: 'posted something new'}
    ]
}

const authReducer = (state = initState, action) => {
    return state
}

export default authReducer